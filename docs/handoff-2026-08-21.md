# Handoff — 2026-08-21

Self-contained briefing for the next agent picking up this branch. Read this fully before acting.

## Task

Make the client test suite green on `feat/personal-category-template` by fixing the
21 pre-existing failures that are unrelated to the just-shipped packing feature:

1. Fix i18n key parity — 17 locales are missing `todo.detail.startDate`.
2. Investigate and fix 4 failing `TripMembersModal` tests (guests / leave-trip / all-have-access).

## Context

Branch `feat/personal-category-template` is ahead of `origin` by 3 commits:

- `097163b8` `feat(todo): default assignee to creator and add start date` (pre-existing)
- `d72ba9a9` `feat(packing): hide sharing UI when a trip has no companion` (just shipped, verified green)
- `55498006` `docs: document shared build prerequisite and local container rebuild`

Working tree is clean. The packing work is done, committed, and its tests are green —
do not modify it. The only remaining work is the two pre-existing failure clusters below.

## Relevant files

- `shared/src/i18n/en/todo.ts` — reference locale; already has `todo.detail.startDate` ("Start date").
- `shared/src/i18n/zh/todo.ts`, `shared/src/i18n/zh-TW/todo.ts` — already have the key ("开始日期" / "開始日期").
- `shared/src/i18n/{de,es,fr,hu,it,tr,ru,nl,id,ar,br,cs,pl,ja,ko,uk,gr}/todo.ts` — the 17 locales MISSING the key.
- `client/tests/unit/i18n/parity.test.ts` — asserts every non-en locale has the exact same merged key set as en; currently failing for those 17 locales.
- `client/src/components/Trips/TripMembersModal.tsx` — members/share modal; guests (#1362), leave-trip, "all have access" live here.
- `client/src/components/Trips/TripMembersModal.test.tsx` — 4 failing tests (FE-COMP-MEMBERS-024…027).

## Current state

**Packing feature (done, do not touch):**
When a trip has only the owner (no members/guests), the packing list hides all
shared/personal UI — the "Shared" and "My list" view pills, the per-item share
control, sharing badges, and category assignee controls. The list defaults to the
user's own list and shows all items. `hasCompanions = tripMembers.length > 1`,
derived from the page-level roster threaded through `ListsContainer → PackingListPanel`
(`usePackingListPanel.ts`).

**Verification so far:**
- `npm run typecheck --workspace=client` — clean.
- Packing component tests — 101 passed.
- `TripPlannerPage.test.tsx` — 56 passed.
- Full client suite — **21 failures in 2 files**, all pre-existing:

| File | Failures | Root cause |
|---|---|---|
| `tests/unit/i18n/parity.test.ts` | 17 | `todo.detail.startDate` added only to en/zh/zh-TW by `097163b8` |
| `TripMembersModal.test.tsx` | 4 | members-modal regression, last touched in `7eabf606` (3.2.0) |

## What was tried

- Ran the full client suite; confirmed the 21 failures are NOT caused by the packing
  commit (packing + planner + todo + offlineDb + shared-contract tests all pass).
- Localized the parity failures to the single missing key `todo.detail.startDate`.
- Localized the 4 member-modal failures: they render the modal in isolation against
  mocked APIs, so the packing/planner changes cannot affect them.

## Decisions

- The packing feature is complete and committed; remaining failures are out of scope
  for it and are handled here as a separate cleanup.
- Do not rework the packing three-tier model to "fix" these — the failures are
  independent.

## Acceptance criteria

- [x] All 22 non-en locales export the exact same merged key set as en.
- [x] `TripMembersModal.test.tsx` passes all 27 tests.
- [x] `npm run test --workspace=client` is fully green.
- [x] `npm run lint --workspace=client` stays 0 errors.

## Constraints

- Do not modify the packing feature files (`client/src/components/Packing/*`,
  `client/src/pages/TripPlannerPage.tsx`) unless a fix genuinely requires it.
- i18n fix: only ADD the missing `todo.detail.startDate` key to locales that lack it;
  do not touch en/zh/zh-TW or any other keys.
- After editing `shared/src/i18n/*`, run `npm run build --workspace=shared` BEFORE
  client tests — the client consumes `shared/dist` (per `AGENTS.md`).
- Client Vitest paths are workspace-relative:
  `npm run test --workspace=client -- src/...` or `tests/...`.
- Commit each fix separately with a conventional-commit message; never force-push.

## Resolution — 2026-08-22

- Added `todo.detail.startDate` to the initially identified 17 locales. The strict
  shared parity gate also found the same missing key in `ca`, `sv`, and `vi`, so
  those three were added as well.
- The four modal failures were caused by the test fixture missing a handler for
  the modal's new `/api/trips/1/invite-link` request. The unhandled request reached
  the auth interceptor and corrupted jsdom's location for later assertions. The
  fixture now returns an empty invite link, and the ambiguous `/Invite/i` assertion
  now targets the exact `Invite` button.
- Verification passed: shared build, strict i18n parity, client tests (203 files;
  3,395 passed and 38 skipped), and client lint (0 errors).
- Fix commits: `66f1c442`, `f683baca`, and `230fa470`; the handoff was recorded in
  `10df1825`.
- PR [#37](https://github.com/syyangv/TREK/pull/37) merged to `main` as
  `46a2abc8`. Stable release `v3.5.7` was deployed to production with image
  digest `sha256:80251a228420872e0ed610b71e5d58103eaf1b1e38bd924940f0424438e3cd77`.
  Deployment run: [32539208925](https://github.com/syyangv/TREK/actions/runs/32539208925).
- The live production health endpoint returned `{"status":"ok"}` after deployment.
